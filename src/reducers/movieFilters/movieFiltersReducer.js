const MOVIE_FILTERS_INITIAL_STATES = {
    cinema: null,
    date: null,
    branch: null
}

export function movieFiltersReducer(state) {
    switch (state) {
        case "CINEMA":
            return {

            }

        case "DATE":
            return {

            }

        case "TIME":
            return {

            }
    }
}